export type JwtType = {
    // userId: string,
    refreshToken: string,
    // expiresAt: Date,
    blocked: boolean
}

export type AccessAndRefreshTokenType ={
    accessToken: string,
    refreshToken: string,
}
